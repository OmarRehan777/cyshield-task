/** @type {import('tailwindcss').Config} */
module.exports = {
	// هنا بنحدد الـ Folders اللي Tailwind بيعملها Scan عشان يلاقي الـ Classes
	content: [
		"./pages/**/*.{js,ts,jsx,tsx,mdx}",
		"./components/**/*.{js,ts,jsx,tsx,mdx}",
		"./app/**/*.{js,ts,jsx,tsx,mdx}",
	],
	theme: {
		extend: {
			// 📝 Custom Colors (الألوان المخصصة)
			// كل لون بنستخدمه بـ Class Name: (مثلاً: bg-primary أو text-black-2)
			colors: {
				// ------------------
				// 1. PRIMARY COLORS (الألوان الأساسية - الأخضر)
				// ------------------
				primary: {
					// اللون الأساسي للأزرار (CTA Buttons) والـ Links المميزة
					DEFAULT: "#B7E08A", // Primary (اللون الأخضر الفاتح)
					// درجة أغمق قليلًا (ممكن نستخدمها في الـ Hover)
					2: "#CFECA8",
					// درجة أفتح (ممكن نستخدمها للخلفيات الخفيفة جداً)
					3: "#E0F5BD",
				},

				// ------------------
				// 2. SECONDARY COLORS (الألوان الثانوية - الأسود والرمادي)
				// ------------------
				black: {
					// أسود غامق جدًا (للعناوين الكبيرة والنصوص الأساسية والخلفيات الغامقة)
					DEFAULT: "#0B0F0E",
					// درجة أسود أفتح (لخلفيات العناصر الفرعية الغامقة زي الـ Footer)
					2: "#1D2322",
					// درجة أسود رمادي (للنصوص الثانوية أو العناصر الـ Shadow)
					3: "#313736",
				},

				grey: {
					// رمادي أساسي (للنصوص والـ Placeholders والـ Icons)
					DEFAULT: "#818B9C", // Gray
					// درجة رمادي أغمق (لحدود العناصر أو خلفيات رمادية خفيفة)
					2: "#9BA6B9", // Gray 2
					// درجة رمادي أفتح (للخلفيات الرمادية الفاتحة جدًا)
					3: "#BCC5D4", // Gray 3
				},

				"light-grey": {
					// رمادي فاتح جدًا (للنصوص الخفيفة والـ Dividers)
					DEFAULT: "#F1F1F1", // Light Gray
					// درجة أفتح من الـ Light Gray (لخلفيات بيضاء تقريباً)
					2: "#F6F6F6", // Light Gray 2
					// أفتح درجة (للخلفيات البيضاء اللي فيها شوية لون)
					3: "#F9F9F9", // Light Gray 3
				},

				// ------------------
				// 3. SUPPORT COLORS (ألوان الدعم - الأحمر/البرتقالي)
				// ------------------
				accent: {
					// اللون الأحمر/البرتقالي (للتنبيهات، الـ FAQ Subtitle)
					DEFAULT: "#E04124", // Accent Red
				},

				// هنا ممكن نضيف الأبيض لو محتاجين نستخدمه كـ Utility Class
				white: "#FFFFFF",
				Black: "#000000",
			},

			// 📝 Custom Typography (الخطوط المخصصة)
			// أنت بتستخدم خط 'Sora' (وده محتاج يكون imported في globals.css)
			fontFamily: {
				// Sora هو الخط الرئيسي للـ Headings والنصوص
				sans: ["Sora", "sans-serif"],
				// لو محتاج خط ثاني للنصوص العادية
				// body: ['Inter', 'sans-serif'],
				playball: ["Playball", "cursive"],
			},
		},
	},
	plugins: [],
};
